name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "integration/**"
      - "release/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - uses: actions/setup-node@60edb5dd545a775178f52524783378180a3fb4bc
        with:
          node-version: "22"
      - name: Run console API tests
        run: node --test test/*.test.js
        working-directory: console/api

  metrics-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - name: Run metrics stack unit tests
        run: bash tests/metrics-stack-unit.sh

  security-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
        with:
          fetch-depth: 0
      - name: Run security PR checks
        run: bash tests/security-pr-checks.sh
